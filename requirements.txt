numpy>=1.24
pandas>=2.0
yfinance>=0.2
scikit-learn>=1.3
xgboost>=2.0
statsmodels>=0.14
scipy>=1.10
matplotlib>=3.7
transformers>=4.40
tqdm>=4.66

wandb>=0.16
optuna>=3.0
pytest>=7.0

# --- PyTorch with CUDA ---
# Install CUDA-enabled PyTorch SEPARATELY (pip install from requirements.txt installs CPU-only):
#   pip install torch>=2.1 --index-url https://download.pytorch.org/whl/cu128
# Or for CUDA 12.4:
#   pip install torch>=2.1 --index-url https://download.pytorch.org/whl/cu124
# Verify: python -c "import torch; print(torch.cuda.is_available())"
